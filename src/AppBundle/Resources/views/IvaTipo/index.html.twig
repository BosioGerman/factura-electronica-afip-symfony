{% extends '::base.html.twig' %}

{% block body -%}
    <h1>IvaTipo list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Afipid</th>
                <th>Valor</th>
                <th>Porcentaje</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('ivatipo_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.afipId }}</td>
                <td>{{ entity.valor }}</td>
                <td>{{ entity.porcentaje }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('ivatipo_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('ivatipo_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('ivatipo_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
