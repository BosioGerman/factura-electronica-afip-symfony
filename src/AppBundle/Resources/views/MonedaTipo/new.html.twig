{% extends '::base.html.twig' %}

{% block body -%}
    <h1>MonedaTipo creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('monedatipo') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
