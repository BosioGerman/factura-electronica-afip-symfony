{% extends '::base.html.twig' %}

{% block body -%}
    <h1>MonedaTipo list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Afipnombre</th>
                <th>Afipid</th>
                <th>Activado</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('monedatipo_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombre }}</td>
                <td>{{ entity.afipNombre }}</td>
                <td>{{ entity.afipId }}</td>
                <td>{{ entity.activado }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('monedatipo_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('monedatipo_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('monedatipo_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
