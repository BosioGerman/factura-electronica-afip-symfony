{% extends 'AppBundle::base.html.twig' %}

{% block body -%}
    <h1>Cliente list</h1>

    <table class="records_list table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Domiciliopostal</th>
                <th>Cuit</th>
                <th>Condicioniva</th>
                <th>Deuda</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('cliente_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombre }}</td>
                <td>{{ entity.domicilioPostal }}</td>
                <td>{{ entity.cuit }}</td>
                <td>{{ entity.condicionIva }}</td>
                <td>{{ entity.deuda }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('cliente_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('cliente_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('cliente_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
